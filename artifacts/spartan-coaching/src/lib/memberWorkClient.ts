export type MemberWorkItem = { id: string; toolId: string; title: string; kind: string; status: string; input: Record<string, unknown>; output: Record<string, unknown>; nextAction?: { title: string; href?: string } | null; updatedAt: string };
export type SaveMemberWork = { toolId: string; title: string; value: string; kind?: "tool_result" | "calculator_report" | "intelligence_brief" | "roleplay" | "transcript" | "resource_work"; status?: "draft" | "completed"; accountId?: string | null; input?: Record<string, unknown>; nextAction?: { title: string; href?: string; dueAt?: string } | null };
async function json(response: Response) { const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || "Saved work is unavailable."); return data; }
export async function saveMemberWork(work: SaveMemberWork): Promise<MemberWorkItem> { return (await json(await fetch("/api/v1/member-work", { method: "POST", credentials: "include", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ kind: work.kind ?? "tool_result", toolId: work.toolId, title: work.title, status: work.status ?? "completed", accountId: work.accountId ?? null, input: work.input ?? {}, output: { text: work.value }, nextAction: work.nextAction ?? null, sourcePlatform: "web" }) }))).item; }
export async function loadMemberWork(): Promise<MemberWorkItem[]> { return (await json(await fetch("/api/v1/member-work", { credentials: "include" }))).items; }
export async function loadMemberWorkItem(id: string): Promise<MemberWorkItem> { return (await json(await fetch(`/api/v1/member-work/${encodeURIComponent(id)}`, { credentials: "include" }))).item; }
